<div class="tg-business-switcher">
    <img src="{{ $gravatarURL }}" class="tg-business-switcher__avatar rounded-circle" alt="{{ $user->name }}">
    <div class="tg-business-switcher__meta">
        <div class="tg-business-switcher__name">{{ $business->name }}</div>
        <div class="tg-business-switcher__user">{{ $user->name }}</div>
    </div>
</div>
